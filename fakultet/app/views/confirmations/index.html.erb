<p id="notice"><%= notice %></p>

<h1>Confirmations</h1>

<table>
  <thead>
    <tr>
      <th>Svrha</th>
      <th>Ime</th>
      <th>Prezime</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @confirmations.to_a.each do |confirmation| %>
      <tr>
        <td><%= confirmation.svrha %></td>
        <td><%= confirmation.ime %></td>
        <td><%= confirmation.prezime %></td>
        <td><%= link_to 'Show', confirmation %></td>
        <td><%= link_to 'Edit', edit_confirmation_path(confirmation) %></td>
        <td><%= link_to 'Destroy', confirmation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <td><%= link_to "download",  confirmation_download_resume_path(:confirmation_id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Confirmation', new_confirmation_path %>


